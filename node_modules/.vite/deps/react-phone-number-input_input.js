import { r as __toESM } from "./rolldown-runtime-B-1-B7_t.js";
import { t as require_react } from "./react.js";
import { a as removePrefixFromFormattedPhoneNumber, b as metadata_min_json_default, c as require_prop_types, d as getCountries$1, f as AsYouType, g as parseDigits, h as parsePhoneNumber$1, i as InputSmart_default, l as formatPhoneNumber$1, m as isValidPhoneNumber$1, n as validateE164Number, p as isPossiblePhoneNumber$1, r as InputBasic_default, s as metadata, t as getInternationalPhoneNumberPrefix, u as formatPhoneNumberIntl$1, v as getCountryCallingCode$1, y as isSupportedCountry$1 } from "./getInternationalPhoneNumberPrefix-0R9Ab2Oi.js";
//#region node_modules/react-phone-number-input/modules/usePhoneDigits.js
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types(), 1);
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
function _slicedToArray(arr, i) {
	return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest();
}
function _nonIterableRest() {
	throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray(o, minLen) {
	if (!o) return;
	if (typeof o === "string") return _arrayLikeToArray(o, minLen);
	var n = Object.prototype.toString.call(o).slice(8, -1);
	if (n === "Object" && o.constructor) n = o.constructor.name;
	if (n === "Map" || n === "Set") return Array.from(o);
	if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}
function _arrayLikeToArray(arr, len) {
	if (len == null || len > arr.length) len = arr.length;
	for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i];
	return arr2;
}
function _iterableToArrayLimit(r, l) {
	var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (null != t) {
		var e, n, i, u, a = [], f = !0, o = !1;
		try {
			if (i = (t = t.call(r)).next, 0 === l) {
				if (Object(t) !== t) return;
				f = !1;
			} else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
		} catch (r) {
			o = !0, n = r;
		} finally {
			try {
				if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return;
			} finally {
				if (o) throw n;
			}
		}
		return a;
	}
}
function _arrayWithHoles(arr) {
	if (Array.isArray(arr)) return arr;
}
/**
* Returns `[phoneDigits, setPhoneDigits]`.
* "Phone digits" includes not only "digits" but also a `+` sign.
*/
function usePhoneDigits(_ref) {
	var _this = this;
	var value = _ref.value, onChange = _ref.onChange, country = _ref.country, defaultCountry = _ref.defaultCountry, international = _ref.international, withCountryCallingCode = _ref.withCountryCallingCode, useNationalFormatForDefaultCountryValue = _ref.useNationalFormatForDefaultCountryValue, metadata = _ref.metadata;
	if (typeof withCountryCallingCode === "boolean" && !(country && international)) console.error("[react-phone-number-input] `withCountryCallingCode` property can only be used together with `country` and `international` properties");
	if (country && defaultCountry) console.error("[react-phone-number-input] When `country` property is passed, `defaultCountry` property has no effect and therefore shouldn't be passed");
	if (typeof international === "boolean" && !country) console.error("[react-phone-number-input] `international` property can only be used together with `country` property");
	var inputFormat = getInputFormat({
		international,
		country,
		defaultCountry,
		withCountryCallingCode
	});
	var countryMismatchDetected = (0, import_react.useRef)();
	var onCountryMismatch = function onCountryMismatch(value, country, actualCountry) {
		console.error("[react-phone-number-input] Expected phone number ".concat(value, " to correspond to country ").concat(country, " but ").concat(actualCountry ? "in reality it corresponds to country " + actualCountry : "it doesn't", "."));
		countryMismatchDetected.current = true;
	};
	var getInitialPhoneDigits = function getInitialPhoneDigits(options) {
		if (value) validateE164Number(value);
		return getPhoneDigitsForValue(value, country, defaultCountry, inputFormat, useNationalFormatForDefaultCountryValue, metadata, function() {
			if (options && options.onCountryMismatch) options.onCountryMismatch();
			for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
			onCountryMismatch.apply(_this, args);
		});
	};
	var _useState2 = _slicedToArray((0, import_react.useState)(country), 2), prevCountry = _useState2[0], setPrevCountry = _useState2[1];
	var _useState4 = _slicedToArray((0, import_react.useState)(defaultCountry), 2), prevDefaultCountry = _useState4[0], setPrevDefaultCountry = _useState4[1];
	var _useState6 = _slicedToArray((0, import_react.useState)(getInitialPhoneDigits()), 2), phoneDigits = _useState6[0], setPhoneDigits = _useState6[1];
	var _useState8 = _slicedToArray((0, import_react.useState)(value), 2), valueForPhoneDigits = _useState8[0], setValueForPhoneDigits = _useState8[1], _useState10 = _slicedToArray((0, import_react.useState)(), 2);
	_useState10[0];
	var setRerenderTrigger = _useState10[1];
	var rerender = (0, import_react.useCallback)(function() {
		return setRerenderTrigger({});
	}, [setRerenderTrigger]);
	function getValueForPhoneDigits(phoneDigits) {
		if (!phoneDigits) return;
		if (inputFormat === "NATIONAL_PART_OF_INTERNATIONAL") phoneDigits = "+".concat(getCountryCallingCode$1(country, metadata)).concat(phoneDigits);
		var asYouType = new AsYouType(country || defaultCountry, metadata);
		asYouType.input(phoneDigits);
		return asYouType.getNumberValue();
	}
	(0, import_react.useEffect)(function() {
		if (value !== valueForPhoneDigits) {
			setValueForPhoneDigits(value);
			setPhoneDigits(getInitialPhoneDigits());
		}
	}, [value]);
	(0, import_react.useEffect)(function() {
		if (country !== prevCountry) {
			setPrevCountry(country);
			var _countryMismatchDetected;
			var _phoneDigits = getInitialPhoneDigits({ onCountryMismatch: function onCountryMismatch() {
				_countryMismatchDetected = true;
			} });
			setPhoneDigits(_phoneDigits);
			if (_countryMismatchDetected) setValueForPhoneDigits(getValueForPhoneDigits(_phoneDigits));
		}
	}, [country]);
	(0, import_react.useEffect)(function() {
		if (defaultCountry !== prevDefaultCountry) {
			setPrevDefaultCountry(defaultCountry);
			setPhoneDigits(getInitialPhoneDigits());
		}
	}, [defaultCountry]);
	(0, import_react.useEffect)(function() {
		if (valueForPhoneDigits !== value) onChange(valueForPhoneDigits);
	}, [valueForPhoneDigits]);
	return {
		phoneDigits,
		setPhoneDigits: (0, import_react.useCallback)(function(phoneDigits) {
			var value;
			if (country) {
				if (inputFormat === "INTERNATIONAL") {
					var prefix = getInternationalPhoneNumberPrefix(country, metadata);
					if (phoneDigits.indexOf(prefix) !== 0) if (phoneDigits && phoneDigits[0] !== "+") phoneDigits = prefix + phoneDigits;
					else if (countryMismatchDetected.current) {} else {
						setPhoneDigits(prefix);
						setValueForPhoneDigits(void 0);
						return rerender();
					}
				} else if (phoneDigits && phoneDigits[0] === "+") phoneDigits = phoneDigits.slice(1);
			} else if (!defaultCountry) {
				if (phoneDigits && phoneDigits[0] !== "+") phoneDigits = "+" + phoneDigits;
			}
			if (phoneDigits) value = getValueForPhoneDigits(phoneDigits);
			setPhoneDigits(phoneDigits);
			setValueForPhoneDigits(value);
		}, [
			country,
			inputFormat,
			defaultCountry,
			metadata,
			setPhoneDigits,
			setValueForPhoneDigits,
			rerender,
			countryMismatchDetected
		]),
		inputFormat
	};
}
/**
* Returns phone number input field value for a E.164 phone number `value`.
* @param  {string} [value]
* @param  {string} [country]
* @param  {string} [inputFormat]
* @param  {string} [defaultCountry]
* @param  {boolean} [useNationalFormatForDefaultCountryValue]
* @param  {object} metadata
* @return {string}
*/
function getPhoneDigitsForValue(value, country, defaultCountry, inputFormat, useNationalFormatForDefaultCountryValue, metadata, onCountryMismatch) {
	if (country && inputFormat === "INTERNATIONAL") {
		var prefix = getInternationalPhoneNumberPrefix(country, metadata);
		if (value) {
			if (value.indexOf(prefix) !== 0) onCountryMismatch(value, country);
			return value;
		}
		return prefix;
	}
	if (!value) return "";
	if (!country && !defaultCountry) return value;
	var asYouType = new AsYouType(void 0, metadata);
	asYouType.input(value);
	var phoneNumber = asYouType.getNumber();
	if (phoneNumber) if (country) {
		if (phoneNumber.country && phoneNumber.country !== country) onCountryMismatch(value, country, phoneNumber.country);
		else if (phoneNumber.countryCallingCode !== getCountryCallingCode$1(country, metadata)) onCountryMismatch(value, country);
		switch (inputFormat) {
			case "NATIONAL": return parseDigits(phoneNumber.formatNational());
			case "NATIONAL_PART_OF_INTERNATIONAL": return parseDigits(removePrefixFromFormattedPhoneNumber(phoneNumber.formatInternational(), getInternationalPhoneNumberPrefix(country, metadata)));
			case "INTERNATIONAL": throw new Error("`inputFormat: \"INTERNATIONAL\"` case should've already been handled earlier in the code");
			case "INTERNATIONAL_OR_NATIONAL": throw new Error("`inputFormat: \"INTERNATIONAL_OR_NATIONAL\"` is not possible when `country` is fixed");
			default: throw new Error("Unknown `inputFormat`: ".concat(inputFormat));
		}
	} else {
		if (phoneNumber.countryCallingCode && phoneNumber.countryCallingCode === getCountryCallingCode$1(defaultCountry, metadata) && useNationalFormatForDefaultCountryValue) return parseDigits(phoneNumber.formatNational());
		return value;
	}
	else return "";
}
function getInputFormat(_ref2) {
	var international = _ref2.international, country = _ref2.country, defaultCountry = _ref2.defaultCountry, withCountryCallingCode = _ref2.withCountryCallingCode;
	return country ? international ? withCountryCallingCode ? "INTERNATIONAL" : "NATIONAL_PART_OF_INTERNATIONAL" : "NATIONAL" : defaultCountry ? "INTERNATIONAL_OR_NATIONAL" : "INTERNATIONAL";
}
//#endregion
//#region node_modules/react-phone-number-input/modules/PhoneInput.js
var _excluded$1 = [
	"Component",
	"country",
	"defaultCountry",
	"useNationalFormatForDefaultCountryValue",
	"value",
	"onChange",
	"metadata",
	"international",
	"withCountryCallingCode"
];
function _extends$1() {
	_extends$1 = Object.assign ? Object.assign.bind() : function(target) {
		for (var i = 1; i < arguments.length; i++) {
			var source = arguments[i];
			for (var key in source) if (Object.prototype.hasOwnProperty.call(source, key)) target[key] = source[key];
		}
		return target;
	};
	return _extends$1.apply(this, arguments);
}
function _objectWithoutProperties$1(source, excluded) {
	if (source == null) return {};
	var target = _objectWithoutPropertiesLoose$1(source, excluded);
	var key, i;
	if (Object.getOwnPropertySymbols) {
		var sourceSymbolKeys = Object.getOwnPropertySymbols(source);
		for (i = 0; i < sourceSymbolKeys.length; i++) {
			key = sourceSymbolKeys[i];
			if (excluded.indexOf(key) >= 0) continue;
			if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue;
			target[key] = source[key];
		}
	}
	return target;
}
function _objectWithoutPropertiesLoose$1(source, excluded) {
	if (source == null) return {};
	var target = {};
	var sourceKeys = Object.keys(source);
	var key, i;
	for (i = 0; i < sourceKeys.length; i++) {
		key = sourceKeys[i];
		if (excluded.indexOf(key) >= 0) continue;
		target[key] = source[key];
	}
	return target;
}
function PhoneInput(_ref, ref) {
	var Component = _ref.Component, country = _ref.country, defaultCountry = _ref.defaultCountry, _ref$useNationalForma = _ref.useNationalFormatForDefaultCountryValue, useNationalFormatForDefaultCountryValue = _ref$useNationalForma === void 0 ? true : _ref$useNationalForma, value = _ref.value, onChange = _ref.onChange, metadata = _ref.metadata, international = _ref.international, withCountryCallingCode = _ref.withCountryCallingCode, rest = _objectWithoutProperties$1(_ref, _excluded$1);
	var _usePhoneDigits = usePhoneDigits({
		value,
		onChange,
		country,
		defaultCountry,
		international,
		withCountryCallingCode,
		useNationalFormatForDefaultCountryValue,
		metadata
	}), phoneDigits = _usePhoneDigits.phoneDigits, setPhoneDigits = _usePhoneDigits.setPhoneDigits, inputFormat = _usePhoneDigits.inputFormat;
	return /*#__PURE__*/ import_react.createElement(Component, _extends$1({}, rest, {
		ref,
		metadata,
		inputFormat,
		international,
		withCountryCallingCode,
		country: country || defaultCountry,
		value: phoneDigits,
		onChange: setPhoneDigits
	}));
}
PhoneInput = /*#__PURE__*/ import_react.forwardRef(PhoneInput);
PhoneInput.propTypes = {
	/**
	* The phone number (in E.164 format).
	* Examples: `"+12"`, `"+12133734253"`.
	* An "empty" `value` could be represented by any "falsy" value like `undefined`, `null` or an empty string `""`.
	*/
	value: import_prop_types.default.string,
	/**
	* A function of `value: string?`.
	* Updates the `value` property (to `undefined` in case it's empty).
	*/
	onChange: import_prop_types.default.func.isRequired,
	/**
	* A two-letter country code for formatting `value`
	* as a national phone number (example: `(213) 373-4253`),
	* or as an international phone number without "country calling code"
	* if `international` property is passed (example: `213 373 4253`).
	* Example: "US".
	* If no `country` is passed then `value`
	* is formatted as an international phone number.
	* (example: `+1 213 373 4253`)
	*/
	country: import_prop_types.default.string,
	/**
	* A two-letter country code for formatting `value`
	* when a user inputs a national phone number (example: `(213) 373-4253`).
	* The user can still input a phone number in international format.
	* Example: "US".
	* `country` and `defaultCountry` properties are mutually exclusive.
	*/
	defaultCountry: import_prop_types.default.string,
	/**
	* If `country` property is passed along with `international={true}` property
	* then the phone number will be input in "international" format for that `country`
	* (without "country calling code").
	* For example, if `country="US"` property is passed to "without country select" input
	* then the phone number will be input in the "national" format for `US` (`(213) 373-4253`).
	* But if both `country="US"` and `international={true}` properties are passed then
	* the phone number will be input in the "international" format for `US` (`213 373 4253`)
	* (without "country calling code" `+1`).
	*/
	international: import_prop_types.default.bool,
	/**
	* If `country` and `international` properties are set,
	* then by default it won't include "country calling code" in the input field.
	* To change that, pass `withCountryCallingCode` property,
	* and it will include "country calling code" in the input field.
	*/
	withCountryCallingCode: import_prop_types.default.bool,
	/**
	* A component that renders the `<input/>` itself and also
	* parses and formats its `value` as the user inputs it.
	* See `InputBasic.js` and `InputSmart.js` for an example.
	*/
	Component: import_prop_types.default.elementType.isRequired,
	/**
	* When `defaultCountry` is defined and the initial `value` corresponds to `defaultCountry`,
	* then the `value` will be formatted as a national phone number by default.
	* To format the initial `value` of `defaultCountry` as an international number instead
	* set `useNationalFormatForDefaultCountryValue` property to `true`.
	*/
	useNationalFormatForDefaultCountryValue: import_prop_types.default.bool,
	/**
	* `libphonenumber-js` metadata.
	*/
	metadata
};
var PhoneInput_default = PhoneInput;
//#endregion
//#region node_modules/react-phone-number-input/modules/PhoneInputBrowser.js
var _excluded = [
	"type",
	"autoComplete",
	"smartCaret",
	"metadata"
];
function _extends() {
	_extends = Object.assign ? Object.assign.bind() : function(target) {
		for (var i = 1; i < arguments.length; i++) {
			var source = arguments[i];
			for (var key in source) if (Object.prototype.hasOwnProperty.call(source, key)) target[key] = source[key];
		}
		return target;
	};
	return _extends.apply(this, arguments);
}
function _objectWithoutProperties(source, excluded) {
	if (source == null) return {};
	var target = _objectWithoutPropertiesLoose(source, excluded);
	var key, i;
	if (Object.getOwnPropertySymbols) {
		var sourceSymbolKeys = Object.getOwnPropertySymbols(source);
		for (i = 0; i < sourceSymbolKeys.length; i++) {
			key = sourceSymbolKeys[i];
			if (excluded.indexOf(key) >= 0) continue;
			if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue;
			target[key] = source[key];
		}
	}
	return target;
}
function _objectWithoutPropertiesLoose(source, excluded) {
	if (source == null) return {};
	var target = {};
	var sourceKeys = Object.keys(source);
	var key, i;
	for (i = 0; i < sourceKeys.length; i++) {
		key = sourceKeys[i];
		if (excluded.indexOf(key) >= 0) continue;
		target[key] = source[key];
	}
	return target;
}
function createInput(defaultMetadata) {
	function PhoneInput(_ref, ref) {
		var _ref$type = _ref.type, type = _ref$type === void 0 ? "tel" : _ref$type, _ref$autoComplete = _ref.autoComplete, autoComplete = _ref$autoComplete === void 0 ? "tel" : _ref$autoComplete, _ref$smartCaret = _ref.smartCaret, smartCaret = _ref$smartCaret === void 0 ? true : _ref$smartCaret, _ref$metadata = _ref.metadata, metadata = _ref$metadata === void 0 ? defaultMetadata : _ref$metadata, rest = _objectWithoutProperties(_ref, _excluded);
		return /*#__PURE__*/ import_react.createElement(PhoneInput_default, _extends({}, rest, {
			type,
			autoComplete,
			metadata,
			ref,
			Component: smartCaret ? InputSmart_default : InputBasic_default
		}));
	}
	PhoneInput = /*#__PURE__*/ import_react.forwardRef(PhoneInput);
	PhoneInput.propTypes = {
		/**
		* HTML `<input/>` `type` attribute.
		*/
		type: import_prop_types.default.string,
		/**
		* HTML `<input/>` `autocomplete` attribute.
		*/
		autoComplete: import_prop_types.default.string,
		/**
		* By default, the caret position is being "intelligently" managed
		* while a user inputs a phone number.
		* This "smart" caret behavior can be turned off
		* by passing `smartCaret={false}` property.
		* This is just an "escape hatch" for any possible caret position issues.
		*/
		smartCaret: import_prop_types.default.bool,
		/**
		* `libphonenumber-js` metadata.
		*/
		metadata: import_prop_types.default.object
	};
	return PhoneInput;
}
createInput();
//#endregion
//#region node_modules/react-phone-number-input/input/index.js
function call(func, _arguments) {
	var args = Array.prototype.slice.call(_arguments);
	args.push(metadata_min_json_default);
	return func.apply(this, args);
}
var input_default = createInput(metadata_min_json_default);
function parsePhoneNumber() {
	return call(parsePhoneNumber$1, arguments);
}
function formatPhoneNumber() {
	return call(formatPhoneNumber$1, arguments);
}
function formatPhoneNumberIntl() {
	return call(formatPhoneNumberIntl$1, arguments);
}
function isValidPhoneNumber() {
	return call(isValidPhoneNumber$1, arguments);
}
function isPossiblePhoneNumber() {
	return call(isPossiblePhoneNumber$1, arguments);
}
function getCountries() {
	return call(getCountries$1, arguments);
}
function getCountryCallingCode() {
	return call(getCountryCallingCode$1, arguments);
}
function isSupportedCountry() {
	return call(isSupportedCountry$1, arguments);
}
//#endregion
export { input_default as default, formatPhoneNumber, formatPhoneNumberIntl, getCountries, getCountryCallingCode, isPossiblePhoneNumber, isSupportedCountry, isValidPhoneNumber, parsePhoneNumber };
